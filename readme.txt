Zip only the files under content_ost to create Archive.zip. Then upload that archive. This way songs will not be nested under an additional folder when client installs the package.

To zip:

make zip # uses -j flag to throw away parent directory path